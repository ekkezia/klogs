import { Button } from '@mui/material';
import { useRouter } from 'next/navigation';
import React from 'react';

interface DistributedSystemsPageProps {
  name: string;
}
const DistributedSystemsPage: React.FC<DistributedSystemsPageProps> = ({
  name,
}) => {
  return <div></div>;
};

export default DistributedSystemsPage;
