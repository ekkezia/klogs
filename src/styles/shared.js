export const MAX_CONTENT_WIDTH = `calc(100% - (${OUTER_BAR_WIDTH} * 2)`;
export const MAX_CONTENT_VIEWPORT_HEIGHT = `calc(100vh - (${HORIZONTAL_BAR_HEIGHT} * 2))`;

// For LOGS
export const LINE1_SIZE = 36 // 0 - 640px 
export const LINE1_SIZE_SM = 48 // > 640px (sm)

// For LOG CONTENT
export const LINE2_SIZE = 27 // 0 - 640px 
export const LINE2_SIZE_SM = 36 // > 640px (sm)
